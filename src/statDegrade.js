
/*
let myVariable = 100;
let fuel = 100// Initialize the variable

const intervalId = setInterval(() => {
    myVariable--; // Decrement the variable by 1
    document.getElementById("demo").innerHTML = "<br>" + "<br>" + myVariable;

    if (myVariable <= 0) {
        clearInterval(intervalId); // Stop the interval when the variable reaches 0 or less
        document.getElementById("demo").innerHTML = "EMPTY";
    }
}, 1000); // Execute every 1000 milliseconds (1 second)
const myForm = document.getElementById('form');
myForm.addEventListener('submit', function (event) {
    event.preventDefault(); // Prevent default form submission

    const nameValue = document.getElementById('input').value;
    const myElement = document.getElementById('radio');

    if (nameValue.includes("fuel")) {
        fuel = 100
        document.getElementById("radio").innerHTML += fuel
    }
}, 10000); // Execute every 1000 milliseconds (1 second)
*/
